#pragma once

#include<vector>
#include<string>

namespace CLRS {
	namespace StringMatching {
		using std::vector;
		using std::string;

		vector<size_t> Rabin_Karp_Matching(const string& text, const string& pattern) {

		}
	}
}